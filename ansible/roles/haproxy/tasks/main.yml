---
- name: Installed haproxy
  apt:
    name: haproxy
    state: present
    update_cache: true

- name: Prepared haproxy config
  template:
    src: haproxy.cfg.j2
    dest: /etc/haproxy/haproxy.cfg
    mode: "0644"
    owner: root
    group: root
  notify:
    - Restarted haproxy
