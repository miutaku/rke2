---
- name: Configure Load Balancer for RKE2 Server Nodes
  hosts: lb
  become_user: root
  become: true
  gather_facts: true
  vars:
    ansible_user: root
  roles:
    - role: common
      tags: common
    - name: haproxy
      tags: haproxy
    - name: keepalived
      tags: keepalived
